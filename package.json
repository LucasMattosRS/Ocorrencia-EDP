{
  "name": "edp-ocorrencia-automation",
  "version": "1.0.0",
  "description": "Automação de preenchimento de ocorrências SGS - EDP",
  "scripts": {
    "dev": "npx live-server src --port=3000 --open=/",
    "build": "mkdir -p dist && cp -r src/* dist/ && echo 'Build completo!'",
    "deploy": "npm run build && netlify deploy --prod --dir=dist",
    "deploy:draft": "npm run build && netlify deploy --dir=dist"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}